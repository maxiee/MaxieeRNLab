import React, {PureComponent} from 'react';
import { ScrollView } from 'react-native';

export default class TemplateHome extends PureComponent {
    static navigationOptions = {
        title: 'Template'
    }

    render() {
        return (
            <ScrollView>
                
            </ScrollView>
        )
    }
}